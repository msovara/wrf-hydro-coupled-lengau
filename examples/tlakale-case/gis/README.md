# Bundled offline copy of NCAR WRF-Hydro GIS preprocessor.
# Clone locally (PC with internet), then scp to Lengau — do not git clone on cluster.
#
#   cd examples/tlakale-case/gis
#   git clone --depth 1 https://github.com/NCAR/wrf_hydro_gis_preprocessor.git
#
# See OFFLINE_DEPLOY.md
